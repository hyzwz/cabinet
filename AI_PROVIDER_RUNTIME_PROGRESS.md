# Historical Document Notice

This document records the 2026-04-15 provider-runtime migration snapshot.

It is preserved as a migration-era reference and commit trail, but it is not the canonical source of truth for Cabinet's current runtime architecture.

For current state, use:

- `README.md` for product-level runtime direction
- `CLAUDE.md` for current engineering guidance
- `PROGRESS.md` for subsequent implementation progress
