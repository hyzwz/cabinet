# Historical Plan Notice

This file is a completed first-stage authorization planning document.

Its implementation goals were completed and then carried forward by later authorization phases, so it should not be treated as the current active plan.

For current state, use:

- `PROGRESS.md` for shipped authorization milestones
- `CLAUDE.md` for current engineering guidance
- later active plans in `plans/` when they remain explicitly in progress
