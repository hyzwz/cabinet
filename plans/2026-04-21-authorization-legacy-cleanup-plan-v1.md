# Historical Plan Notice

This file is a legacy-cleanup authorization plan preserved for historical reference.

Its primary purpose was to retire the old authorization path after the unified authorization system became the main execution path. It should not be treated as the current active plan.

For current state, use:

- `PROGRESS.md` for shipped authorization milestones
- `CLAUDE.md` for current engineering guidance
- active plans in `plans/` for unfinished follow-up work
