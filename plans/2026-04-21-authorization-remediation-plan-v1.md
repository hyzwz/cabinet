# Historical Plan Notice

This file is a remediation-stage authorization plan kept for historical reference.

Its key fixes were carried forward into later authorization work, so it should not be treated as the current active planning document.

For current state, use:

- `PROGRESS.md` for shipped authorization milestones
- `CLAUDE.md` for current engineering guidance
- the current active plans in `plans/` for unfinished work
