# Historical Design Note

This document is a focused design note for editor agent visibility and cabinet scoping from 2026-04-13.

It is preserved as historical reasoning for that change area, but it should not be treated as the primary source of truth for the current editor architecture.

For current state, use:

- `PROGRESS.md` for implemented changes
- current code in the editor, agents, and conversation routing layers
