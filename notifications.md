# Historical Document Notice

This document describes an earlier in-app toast notification design centered on conversation completion toasts.

Cabinet now has a broader collaboration notification system, so this file is preserved only as historical reference and should not be used as the current notification architecture guide.

For current state, use:

- `PROGRESS.md` for shipped collaboration phases
- relevant implementation files under `src/lib/collaboration/` and notification UI surfaces
