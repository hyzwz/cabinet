# Historical Notice: AI Claude Editor Runtime Notes

This document is retained only as a historical runtime migration note.

It captured the 2026-04-15 transition period where Cabinet was moving from Claude-only, PTY-first execution toward structured adapters plus interactive terminal surfaces. That direction remains broadly relevant, but the file is no longer the canonical description of the current runtime architecture.

For current runtime behavior and product/runtime boundaries, use these sources instead:

- `README.md`
- `CLAUDE.md`
- `PROGRESS.md`
- active plans under `plans/`

Do not treat this file as the authoritative current-state runtime reference.
